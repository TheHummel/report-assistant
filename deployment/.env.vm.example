# Environment variables for VM deployment
# Copy this to .env on the VM

# PostgreSQL
POSTGRES_PASSWORD=change-this-super-secret-password

# JWT Secret (min 32 characters)
JWT_SECRET=change-this-super-secret-jwt-token-with-at-least-32-characters-long

# Anon Key (must be a JWT token with role=anon, generated using the JWT_SECRET above)
ANON_KEY=eyXXX

# Service Role Key (must be a JWT token with role=service_role, generated using the JWT_SECRET above)
SERVICE_ROLE_KEY=eyXXX

# Site URL (your app URL)
SITE_URL=https://epr-latex-report-assistant.app.cern.ch

# Additional redirect URLs (comma-separated)
ADDITIONAL_REDIRECT_URLS=https://epr-latex-report-assistant.app.cern.ch/**
